{
  "EXAMPLE_API_KEY": "replace-me-with-a-real-value",

  "_comment_survive_branch": "LIVE, non-paper credentials -- see ARCHITECTURE.md section 19. Never reuse ALPACA_API_KEY/ALPACA_API_SECRET/ALPACA_ENDPOINT (the existing paper-only fleet credentials) for these. Only add these once a real, human-KYC'd live Alpaca account actually exists and is funded.",
  "ALPACA_SURVIVE_LIVE_KEY": "replace-me-with-a-real-LIVE-value",
  "ALPACA_SURVIVE_LIVE_SECRET": "replace-me-with-a-real-LIVE-value",
  "ALPACA_SURVIVE_LIVE_ENDPOINT": "https://api.alpaca.markets/v2",

  "_comment_survive_email": "One AgentMail developer account + API key covers EVERY future citizen's own inbox, created automatically via API -- see survive-email.js round 5. No credit card required on AgentMail's free tier (caps at 3 inboxes).",
  "SURVIVE_AGENTMAIL_API_KEY": "replace-me-with-a-real-value",

  "_comment_survive_spending": "How citizens spend real money on non-trading work -- see city-spending.js round 10 (revised to Stripe Issuing after the user rejected a linked-account model: 'I just want a flat balance that I can put in there'). A sole-proprietor/individual Stripe account (SSN, no LLC/EIN) with Issuing enabled; cards draw ONLY from Stripe's own v2 storage FinancialAccount balance -- a genuinely isolated, flat, prepaid pool, never a live pull against a personal bank/card. Use a TEST key (sk_test_...) until the whole flow is proven, exactly like Alpaca's paper-before-live discipline.",
  "SURVIVE_STRIPE_SECRET_KEY": "replace-me-with-a-real-value",
  "SURVIVE_STRIPE_PUBLISHABLE_KEY": "replace-me-with-a-real-value"
}
